apiVersion: v1
kind: Service
metadata:
    name: "kubernetestest-e9d2"
    labels:
        app: "kubernetestest-e9d2"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "kubernetestest-e9d2"