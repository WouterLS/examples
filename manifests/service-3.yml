apiVersion: v1
kind: Service
metadata:
    name: "kubernetestest-1111"
    labels:
        app: "kubernetestest-1111"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "kubernetestest-1111"