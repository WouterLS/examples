apiVersion: v1
kind: Service
metadata:
    name: "kubernetestest-bbc8"
    labels:
        app: "kubernetestest-bbc8"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "kubernetestest-bbc8"